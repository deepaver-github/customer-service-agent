# Special Care Australia — AI Care Assistant

## What This Is

A customer-facing AI assistant for **Special Care Australia**, an NDIS-registered disability support provider. Participants, families, support coordinators, and staff chat with the assistant to look up participant records, find services, ask NDIS questions, and reach a human when needed.

Backend is a FastAPI app using Claude for natural language; frontend is an Angular SPA served from the same origin. This database is the **system of record** for SCA operations (participants, plans, shifts, notes, incidents) — billing is intentionally out of scope and handled by external software (Brevity/Lumary/etc.).

## Tech Stack

- **Backend:** Python 3.10+, FastAPI (async), SQLAlchemy 2.0 (async) + asyncpg, Pydantic BaseSettings
- **LLM:** Claude via Anthropic Python SDK (`anthropic`, model `claude-sonnet-4-6`)
- **Database:** PostgreSQL 16 via Docker Compose (SQLite/aiosqlite in tests)
- **Frontend:** Angular 20 (standalone components, signals) + Tailwind CSS v3, marked + DOMPurify for markdown, `@microsoft/fetch-event-source` for SSE
- **Config:** `.env` (secrets) + `agent_config.yaml` (agent behavior)
- **Testing:** pytest + pytest-asyncio (65 passing, 4 known-failing streaming-mock tests unchanged from main)
- **Logging:** structlog (structured JSON)
- **Build:** hatchling via `pyproject.toml`; Angular via `@angular/build:application`

## Architecture

Monolithic FastAPI app + Angular SPA in the same deploy unit (Angular builds into `app/static/`, FastAPI serves it at root).

```
app/
  main.py              # FastAPI app, lifespan, CORS, static SPA mount at "/"
  config.py            # Pydantic BaseSettings + YAML config loader
  api/
    models.py          # Request/response Pydantic models (incl. SessionList*, participant_id/staff_id on Session)
    dependencies.py    # FastAPI DI (AgentService singleton)
    routes/
      chat.py          # POST /chat, POST /chat/stream (SSE)
      sessions.py      # Session CRUD, GET /sessions (cursor-paginated)
  agent/
    service.py         # Core agent — Claude API calls + tool-use loop (streaming + non-streaming)
    prompt.py          # System prompt builder (SCA personality + NDIS context from YAML)
    tool_executor.py   # Dispatches tool calls, injects AsyncSession, handles errors
  tools/
    registry.py        # @register_tool decorator, JSON Schema generation
    examples/
      lookup_participant.py  # Find by NDIS number / name / email
      get_contacts.py        # Participant's family, plan manager, coordinator, GP, emergency
      search_knowledge.py    # Keyword search against knowledge_articles
  memory/
    database.py        # Async engine/session factory + audit-trigger SQL applied on init
    models.py          # All SQLAlchemy models (see Schema section)
    repository.py      # Async DB methods (sessions, participants, contacts, knowledge)
    seed.py            # NDIS sample data (3 participants, 2 staff, etc.) — run via `python -m app.memory.seed`
  escalation/
    handler.py         # Escalation tool + webhook dispatch
  static/              # Angular build output — gitignored, generated by `cd frontend && npm run build`
frontend/
  src/app/
    app.ts             # Shell composing sidebar + chat view
    components/
      session-list/    # Sidebar with grouped sessions, search filter
      chat-view/       # Thread + composer
      message-item/    # Variants: user / assistant / escalation
      message-composer/
      tool-card/       # Collapsible tool call card
    services/
      chat.store.ts    # Signal-based store (sessions, turns, isStreaming, isEscalated)
      chat.service.ts  # SSE streamMessage via fetchEventSource + AbortController
      session.service.ts
    models/            # TypeScript types matching backend response shapes
    utils/             # markdown.util, timeago.util
  public/              # logo.png + favicon variants pulled from specialcareaust.com.au
  proxy.conf.json      # Dev proxy /chat, /sessions, /health → :8001
  tailwind.config.js   # Brand colors (#1e7e2d green, #7ebec5 teal); Poppins + Open Sans fonts
tests/
  conftest.py          # Fixtures: in-memory DB, mock Anthropic client, helpers
  test_tool_registry.py
  test_memory.py
  test_agent_service.py
  test_escalation.py
  test_chat_api.py
  test_list_sessions.py
docker-compose.yml     # postgres:16 — user=agent, db=customer_service, port 5432
```

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| POST | /chat | Send message, get response |
| POST | /chat/stream | Send message, get SSE stream |
| POST | /sessions | Create session (optional `participant_id` / `staff_id`) |
| GET | /sessions | Cursor-paginated session list with last-message preview |
| GET | /sessions/{id} | Get session + message history |
| DELETE | /sessions/{id} | Close a session |
| GET | /health | Health check |
| GET | / (and any non-API path) | Angular SPA (`app/static/index.html`) |

## Schema

System of record for SCA. Phase 1 tables are live; Phases 2–4 are designed but unimplemented (see plan at `C:\Users\deepa\.claude\plans\lucky-sprouting-globe.md`).

**Live now:**

- `sessions`, `messages` — chat history. `sessions` has nullable `participant_id` (FK to participants) and `staff_id` (FK to staff).
- `participants` — NDIS number, name, DOB, primary disability category, address, comms needs, status. Soft-deleted via `deleted_at`. Partial unique index on `ndis_number WHERE deleted_at IS NULL` (Postgres only).
- `contacts` — per-participant: family, guardian, support_coordinator, plan_manager, GP, advocate, emergency. `is_primary` / `is_emergency` flags.
- `staff` — name, role, NDIS Worker Check / WWCC / police check expiries.
- `participant_staff_assignments` — primary/secondary/do_not_roster with valid_from/to.
- `service_types` — catalog (SIL, personal_care, community_participation, respite, complex_support). No rates — billing is out of scope.
- `knowledge_articles` — markdown body, category, tags. Replaces the old FAQ table; powers `search_knowledge`.
- `audit_log` — append-only, populated by Postgres triggers on participants/contacts/staff/assignments. Generic shape: `table_name`, `record_id`, `action`, `old_values` JSON, `new_values` JSON. Trigger function `audit_trigger_func()` is defined in `app/memory/database.py` and applied to each audited table via `_trigger_sql(table)`.

**Planned (not yet implemented):** plans, goals, plan_goals, service_agreements, shifts, progress_notes, incidents, incident_followups, medication_records, medication_administration_log, restrictive_practices, risk_assessments, participant_preferences, documents. Two new trigger types ship with them: audit triggers (existing pattern, more tables) and an immutability trigger that blocks UPDATE/DELETE on locked progress notes and on medication administration logs unconditionally.

## Tools (chat-facing)

Currently enabled in `agent_config.yaml`:

- `lookup_participant(ndis_number?, name_or_email?)` — single or multiple match
- `get_contacts(participant_id)` — primary + emergency first
- `search_knowledge(query)` — keyword match against published articles
- `escalate(reason)` — hand off to a human team member

All tools take `db: AsyncSession` (injected by `tool_executor.py`), return plain dicts. Errors return `{"error": "..."}`.

## Key Design Decisions

- **No LangChain** — Claude's native tool-use API. Simpler, easier to debug.
- **Decorator-based tool registration** — `@register_tool` auto-converts type hints to JSON Schema.
- **Escalation as a tool** — Claude calls `escalate` like any other tool. Cleaner than parsing JSON from text.
- **Audit via Postgres triggers, not Python** — every audited table gets a row in `audit_log` automatically on INSERT/UPDATE/DELETE. Soft-delete (`deleted_at` update) is itself audited because it's an UPDATE.
- **System of record, billing excluded** — SCA's operational truth lives here; billing flows go through external tools that consume exported shift records.
- **No Alembic yet** — tables auto-created via `Base.metadata.create_all` on startup. Schema changes require `docker compose down -v && docker compose up -d && python -m app.memory.seed`. **Add Alembic before any production data lands.**
- **Single-origin SPA + API** — Angular builds into `app/static/`, FastAPI serves it via `StaticFiles(html=True)` mounted at `/`. CORS allows `http://localhost:4200` for dev (Angular dev server).
- **Single deployment** — not multi-tenant. Branding (logo, colors, agent personality) is hardcoded for SCA.

## Brand & Visual Identity

- Source of truth: https://specialcareaust.com.au/
- Accent green `#1e7e2d`, teal `#7ebec5`, body text `#111827`
- Fonts: Poppins (display) + Open Sans (body) from Google Fonts
- Logo files in `frontend/public/`: `logo.png` (wordmark + hand), `favicon.png` / `favicon-32.png` / `apple-touch-icon.png` (square hand icon for tabs and small avatars)
- Agent personality in `agent_config.yaml` introduces itself as the "Care Assistant for Special Care Australia", uses Australian English, frames everything around NDIS supports

## Configuration

- **`.env`** — secrets: `ANTHROPIC_API_KEY`, `DATABASE_URL` (defaults to local Postgres), `ESCALATION_WEBHOOK_URL` (optional), `LOG_LEVEL`
- **`agent_config.yaml`** — agent behavior: personality, model, max_tokens, enabled tools, memory settings, escalation rules

## Running

```bash
# 1. Start PostgreSQL (foreground or detached)
docker compose up -d

# 2. Activate venv
.venv\Scripts\activate

# 3. First-time setup or after a schema change: reset volume + seed
docker compose down -v && docker compose up -d
python -m app.memory.seed     # creates tables + triggers, inserts NDIS samples

# 4. Run the backend
uvicorn app.main:app --reload --port 8001

# 5. Frontend dev (separate terminal — proxies API calls to :8001)
cd frontend && npm start      # http://localhost:4200

# OR — production frontend build served by FastAPI directly:
cd frontend && npm run build  # builds into ../app/static/
# then visit http://localhost:8001

# Tests
pytest -v                     # 65 pass, 4 known-failing streaming-mock tests
```

Port 8000 is blocked on this machine — use **8001**.

## IntelliJ Debug Setup

- Run/Debug config: Module name `uvicorn`, Parameters `app.main:app --reload --port 8001`
- Working directory: project root
- Interpreter: `.venv\Scripts\python.exe`
- Remove `--reload` when using breakpoints (reload spawns a child process)

## Gitignored / not committed

- `app/static/*` — Angular build artifacts (content-hashed; would create noisy diffs). `app/static/.gitkeep` placeholder lives in the repo so the directory exists for the FastAPI static mount.
- `frontend/node_modules/`, `frontend/dist/` — standard Angular ignores
- `.venv/`, `.env`, `*.db`, `__pycache__/`

Anyone cloning fresh needs `cd frontend && npm ci && npm run build` before `uvicorn`, or FastAPI will 404 the SPA bundle.

## Design Docs

- Original e-commerce agent spec (historical): `docs/superpowers/specs/2026-05-24-customer-service-agent-design.md`
- Current NDIS schema plan (Phase 1 done, Phases 2–4 pending): `C:\Users\deepa\.claude\plans\lucky-sprouting-globe.md`

## What's Not Done Yet

- **NDIS schema Phases 2–4** — plans/goals/agreements, shifts, clinical+safeguarding (progress notes, incidents, medications, restrictive practices, risk assessments, documents). See the plan file above.
- **Alembic migrations** — must add before real participant data lands.
- **Authentication / authorization** — anyone hitting the API can read any participant.
- **Rate limiting**
- **Document storage backend** — `documents.storage_url` (Phase 4) is a string pointer; S3/blob wiring is a separate decision.
- **Production deployment config** — single-origin static mount is solid, but no Dockerfile for the app itself, no env-var injection beyond `.env`, no health-check probe wiring.
- **NDIS portal integration / claim generation** — explicitly out of scope (handled by external billing software).
